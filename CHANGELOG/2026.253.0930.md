# 2026.253.0930 — fix(omarchy): single-line the omarchy-rejects-unknown check description

## Summary

The omarchy-rejects-unknown check description was a plain multi-line scalar — the remote candy scan (the deploy-time remote candy parse) failed on it (yaml line 36: could not find expected colon). Single-lined.

## How tested (pasted)

```
$ python3 -c "import yaml; yaml.safe_load(open('charly.yml'))" && echo YAML-OK
YAML-OK
```

## Rulebook compliance

- R0: git-workflow loaded.
- R1: the remote scan failure traced to the plain multi-line scalar in the check description.
- R6: no force-push, no hook bypass, PR-only landing.
- R10: the parse check pasted.

## Change classification

- Change class: fix (the candy YAML parse)
- Verification gate: the parse check (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
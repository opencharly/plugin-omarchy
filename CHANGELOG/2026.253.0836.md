# 2026.253.0836 — feat(omarchy): the verb uses the shared verdict pipeline — expect_non_zero + the step-level matchers (R3)

## Summary

The omarchy verb now uses the shared verdict pipeline (R3 — mirrors plugin-command):

- **expect_non_zero** (new input field): asserts the command FAILED (any non-zero exit) — the CLI-rejects-X class (e.g. "omarchy theme set invalid" must fail).
- **exit_status / stdout / stderr** (the step-level modifiers, riding the base #Op): the exact exit code + the shared sdk.MatchAll matchers.

Before: the verb hand-rolled a weaker exit-0-only check that never applied the step-level matchers — a bed's stdout/stderr assertions were silently unenforced (a latent defect in the merged verb).

## How tested (pasted)

**The live proof (the golden VM, the new plugin loaded, the omarchy self-checks in the plan) — ZERO failures:**

```
$ charly check live check-omarchy-eval-base-inst
  PASS  check the omarchy verb reports the installed version (the verdict pipeline)  [omarchy-version]  omarchy version: exit=0
  PASS  check the omarchy verb asserts the CLI's failure behavior (expect_non_zero)  [omarchy-rejects-unknown]  omarchy definitely-not-a-command: exit=127
  PASS  check the record verb dispatches through the provider registry ...  [record-verb-dispatches]  No active recordings
33 steps: 21 passed, 0 failed, 12 skipped
```

The changed paths executed live against a real omarchy system: the verb ran the installed CLI (exit=0 + the stdout matcher passed) and asserted the CLI's failure behavior (exit=127 + the stderr matcher passed). The earlier 1-failed run was the record plugin absent from the test plugin dir — fixed by loading it; the clean run is pasted.

**The unit suite:**

```
$ go test -count=1 .
ok  	github.com/opencharly/plugin-omarchy/candy/plugin-omarchy	0.003s

$ go vet ./... && gofmt -l .
VET-OK
FMT-CLEAN
```

9 tests cover the verdict pipeline: RunsCommand, NonZeroExitFails, EmptyArgsFails, ExpectNonZeroPasses, ExpectNonZeroFailsOnZero, ExitStatusMatcher (exit 1 with exit_status 1 → pass; exit 0 with exit_status 1 → fail), StdoutMatcher (contains pass/fail), StderrMatcher.

## Rulebook compliance

- R0: the plugin skill loaded; the canonical in-proc verdict pattern (plugin-command) followed.
- R1: the latent defect (unenforced step matchers) traced to the hand-rolled exit-0-only check; the earlier 1-failed gate run traced to the record plugin absent from the test plugin dir (fixed, clean run pasted).
- R3: the verb now uses the shared sdk.MatchAll + op.ExitStatus — no duplicated matcher logic.
- R4: no workarounds — expect_non_zero is the honest any-nonzero assertion.
- R6: no force-push, no hook bypass, PR-only landing.
- R10: the clean live proof + the unit tests pasted.

## Change classification

- Change class: feat (the verdict pipeline + expect_non_zero)
- Verification gate: the clean live proof + the unit tests (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
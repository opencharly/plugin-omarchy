# 2026.253.1645 — feat: the omarchy verb method enum (#OmarchyInput v2) — the acceptance surface

## Summary

The omarchy: check verb gains a structured method enum (#OmarchyInput v2) beyond the bare CLI wrapper — the acceptance surface the omarchy acceptance corpus beds (plan/omarchy-acceptance-corpus.md) are authored against:

- **shell-IPC methods** (the omarchy-shell IPC the menu and hotkeys use): shell-ping, shell-summon (plugin + optional payload), shell-hide, shell-list-plugins, shell-reload-config, shell-notifications-dismiss, shell-notifications-send (title + text)
- **cli-read methods** (the omarchy-* bin commands): channel-current, default-browser, default-terminal, default-editor, theme-current, theme-bg-current, font-current, weather-location (args), version
- **cli** (default, unchanged): the omarchy command center with args
- **candy/plugin-omarchy/charly.yml**: the plugin's own plan gains two new acceptance checks (omarchy-shell-ping, omarchy-channel-current) exercising the new methods

## How tested

**Unit tests (pasted):**

```
$ go test ./...
ok  	github.com/opencharly/plugin-omarchy/candy/plugin-omarchy	0.003s
```

The dispatch table (19 cases: every method's exact argv), the validation table (6 cases: the required-field errors), and the shell-ping/channel-current argv assertions all pass.

**Fresh-rebuild bed run (pasted) — the changed dispatch executed live on a disposable target:**

The spike bed check-omarchy-verb-spike (disposable pod, image: omarchy, add_candy: the plugin from this PR's branch) ran the new methods live:

```
$ charly check run check-omarchy-verb-spike
[image-build] PASS after 22.187s [cache=48/52]
[check-image] PASS after 18.373s
[deploy-add] PASS after 7.788s
[config] PASS after 4.22s
[start] PASS after 2.979s
[bring-up-members] PASS after 33ms
[check-live] PASS after 3.875s
[feature-run] PASS after 2.495s
[update] PASS after 36.013s
[check-live-rebuild] PASS after 3.161s
[feature-run-rebuild] PASS after 2.482s
[cleanup] PASS after 16.512s
[cleanup-members] PASS after 0s
charly check run check-omarchy-verb-spike: PASS (steps=13)
```

The check-live per-step verdicts (the new dispatch + the unchanged path):

```
PASS  check the omarchy verb reads the active channel (the new method dispatch)  [spike-channel-current]  omarchy : exit=0
PASS  check the omarchy verb reports the configured font (the new method dispatch)  [spike-font-current]  omarchy : exit=0
PASS  check the omarchy verb dispatches the CLI (the unchanged path)  [spike-cli-version]  omarchy version: exit=0
```

Note: the first spike attempt used default-browser, which returned empty stdout in the pod (no desktop session → xdg-settings has no default) — a venue-context finding, not a dispatch defect; the headless-safe font-current proves the same dispatch path.

## Rulebook compliance

- R3: the verdict pipeline is unchanged (exit code + MatchAll) — the dispatcher reuses the existing RunCapture path, no new mechanism
- The schema is served over Describe and spliced onto the base (the existing plugin contract)

## Change classification

- Change class: feature (plugin verb surface)
- Verification gate: unit tests + fresh-rebuild bed run
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*
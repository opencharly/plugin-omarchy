# 2026.253.0925 — ci(omarchy): install the org-wide tag-on-merge dispatcher

## Summary

Installs the org-wide tag-on-merge dispatcher in the plugin-omarchy repo — the repo was missing it, so its merges went untagged (PR #2 merged with no tag, and the eval-omarchy golden's plugin-omarchy pin could not resolve). The workflow is ported verbatim from plugin-check (R3 — one canonical copy; the org-wide dispatcher is installed in every opencharly repo).

## How tested (pasted)

```
$ diff plugin-check/.github/workflows/tag-on-merge.yml plugin-omarchy/.github/workflows/tag-on-merge.yml && echo IDENTICAL
IDENTICAL
$ ls .github/workflows/
deploy.yml pr-validator.yml tag-on-merge.yml
```

The workflow self-heals: on its next validator-completion trigger it scans the last 24h of merged PRs and tags the missed ones (PR #2 included).

## Rulebook compliance

- R0: git-workflow loaded.
- R3: the org-wide dispatcher ported verbatim — no forked copy.
- R6: no force-push, no hook bypass, PR-only landing.
- R9: the tag discipline depends on this dispatcher; this is its canonical install.
- R10: the identical-file check pasted.

## Change classification

- Change class: ci (the tag-on-merge install)
- Verification gate: the identical-file check (pasted)
- Attribution tier: fully tested and validated

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (fully tested and validated)*